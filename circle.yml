machine:
  python:
    version: 3.5.2

dependencies:
  cache_directories:
    - frontend/node_modules
  override:
  - pip install -e .[test]
  - cd frontend && npm install

test:
  override:
  - pytest yawn
  - flake8 yawn
