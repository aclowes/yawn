machine:
  python:
    version: 3.5.2

dependencies:
  cache_directories:
    - frontend/node_modules
  override:
    - pip install -e .[test]
    - cd frontend && npm install

test:
  override:
    - pytest --cov=yawn yawn
    - flake8 yawn
  post:
    - bash <(curl -s https://codecov.io/bash)
